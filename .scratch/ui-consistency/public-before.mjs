import {chromium} from '@playwright/test';
import {writeFile} from 'node:fs/promises';
const browser=await chromium.launch({channel:'chrome',headless:true});const context=await browser.newContext({viewport:{width:390,height:900}});const page=await context.newPage();const styles=[];page.on('response',r=>{if(r.url().endsWith('.css'))styles.push({url:r.url(),status:r.status()})});await page.goto('http://127.0.0.1:3300/login');await page.waitForTimeout(700);await page.screenshot({path:'docs/design/evidence/ui-consistency/before-rich/login-direct-390.png'});const result=await page.evaluate(()=>({headerDisplay:getComputedStyle(document.querySelector('.public-header')).display,headerPadding:getComputedStyle(document.querySelector('.public-header')).padding,mainPadding:getComputedStyle(document.querySelector('main')).padding,overflow:document.documentElement.scrollWidth>innerWidth}));console.log({styles,result});await writeFile('docs/design/evidence/ui-consistency/before-rich/login-direct.json',JSON.stringify({styles,result},null,2));await browser.close();

# Portfolio allocation completion

Astra direction, 2026-09-06. Luna implements ticket 20. Mode: Operate. Extend the existing `PortfolioExposure` section and DESIGN.md; preserve the accepted holdings and seven-bucket meter presentation.

After the seven cost-exposure buckets and their data-quality explanation, add a separated section titled “Market context comparison” (with equivalent Traditional/Simplified Chinese). Lead with readable market-state and allocation-mode labels, followed by the observation date and the source's explanation. The comparison is contextual information from the existing deterministic policy; it is not a trade action or a new personalized calculation.

Show the three combined categories in a semantic table with category, current percentage, target percentage, difference in percentage points and a readable underweight/balanced/overweight status. Header and cells use the existing 14/16px type and tabular numerals; meaningful signs stay visible. Use thin rules and 24px section padding, with no new color roles or decorative chart. At 390px, retain all columns within the existing named, focusable local scroll pattern, leaving the page width intact. Current and target must be visually comparable and clearly named; do not label percentage-point differences as relative percent changes.

Keep cost-basis and category-combination semantics visible: full cost includes unclassified positions, while target comparison covers only the existing three combined categories. Cash proxies do not represent a bank cash balance. Do not normalize away unknown holdings or replace missing prices with zero. Continue showing warnings for excluded invalid costs and concentration. Empty holdings keep the existing first-use state rather than inventing an allocation result.

Unknown/stale/absent market context keeps the current buckets readable and states that target comparison is unavailable. Do not display fallback targets as a current recommendation. A market-reader failure is distinct from a failure to load the owner's holdings; preserve the source's best-effort market-context behavior. Retry, logout cleanup and three locales follow existing components. Avoid untranslated API enums or English-only policy text in translated UI; map only documented deterministic vocabulary and retain the API's valid contract.

Implementation uses the existing sectors dashboard reader, beta policy and `compareExposureToTarget`; no second allocation engine. The frozen `server/api/stocks/exposure.get.ts` is the behavioral reference: holdings first, optional sectors context, unknown means no gaps, context failure leaves exposure usable, successful context supplies allocation and lastUpdated. Match source date serialization and the new reader's freshness rules.

Acceptance needs disposable PostgreSQL evidence for known-state allocation/gaps, unknown/stale/absent context, market-reader failure isolation, and unchanged owner/cost behavior. Keep the existing policy/concentration fixtures. In one desktop/light and mobile/dark browser batch, read the real comparison, follow the relevant existing holdings/Company destination, and verify unknown/retry/locale behavior. Astra reviews the final two captures. This brief records direction only, not completion.

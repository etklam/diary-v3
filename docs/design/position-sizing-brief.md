# Position sizing — Astra direction

2026-09-06. Mode: Operate. Ticket 47 implementation remains pending. Luna owns implementation; Astra owns composition and acceptance. Reuse DESIGN.md and the accepted FIRE calculator's input/result rhythm.

The user is deciding how to split an intended position into batches and then record that intention. Use one heading and a compact explanation, followed by an input column and a wider results column. At tablet/mobile widths stack inputs, results, then handoff actions. Inputs use native labelled controls: available capital, intended stock price, reserve percentage, one of the four existing strategies and rounding mode. Optional symbol/context needed by handoff belongs beside those inputs. Do not imply a live quote lookup if only a hypothetical price was entered.

Preserve the source strategies: pyramid 40/30/20/10, pyramid variant 20/20/30/20/10, rectangular 30/30/30/10, inverted pyramid 10/20/30/40. Show each strategy's ratio sequence and translated explanation with the selection; avoid four large decorative cards. Rounding retains down/nearest/up and its source batch behavior. The selected strategy can update the preview immediately without losing capital or price input.

Results begin with invested amount and total shares, then a compact breakdown of reserved cash, unallocated cash and total remaining cash. These three values have distinct labels. The batch table keeps ratio, planned amount, whole shares, actual amount and cumulative shares/amount. Tabular numerals, 16px body, 14px metadata, 24px page heading and 18px section headings follow the existing system. Use thin rules and 24–40px spacing. The wide table scrolls only inside a named keyboard-focusable region on mobile.

Show invalid/zero input as an instruction beside the relevant field; no NaN, infinity or fabricated zero summary. Preserve source warnings about ratio totals, adjusted last batch and actual overbudget results, with readable translated text. Do not silently clamp valid source rounding results or hide a warning. Reject non-finite, negative-ratio or out-of-range reserve inputs as intentional validation corrections; preserve valid source numeric results with fixed fixtures.

Handoff actions sit after the results: Copy Markdown, Save to Diary (new versus explicit append), and Prepare Trade Plan. Use the existing Diary API and Trade Plan prefill mechanism; preparing a plan opens its editor and does not submit a trade or mutate the ledger. The source passes total invested into maxPositionSize; preserve its documented monetary unit instead of changing it to shares. Review all source fields before wiring. Authentication keeps a safe return target, failed saves retain inputs, date conflict exposes the existing open/append choice, and clipboard failure provides selectable Markdown.

No new API/table for calculations, no generated advice and no image asset. Keep the calculations in a native-compatible domain module. Test actual numeric outputs rather than porting source tests that merely search file text. Browser evidence must demonstrate the real Diary create/append and Trade Plan prefill paths, invalid input, switching strategies, copy/recovery, and desktop/light plus 390px/dark. Astra inspects the final pair and any actual defects are fixed in one batch.

# Market state finish review

## Implementation evidence — 2026-09-06

Ticket44's vertical slice is implemented and awaits root acceptance. No production service, live provider, or real user data was used.

- **Domain and warmup:** The source-derived daily move, 40-day breadth, 5/10-day ratio, regime and score formulas are preserved. Known daily moves remain visible during warmup; ratios stay nullable until their eligible windows exist; partial 40-day percentages remain visible, while regime and score require at least 90% of the configured universe to have a 40-day series. The 39/40 observation and 8/10→9/10 exact-90% boundaries are covered in `tests/unit/market-state.test.ts` and recorded in [ADR 0008](../adr/0008-market-state-warmup-and-freshness.md).
- **Persistence and jobs:** `market_universe` and `market_breadth_daily` use the exact nullable/decimal fields and date constraints. Price persistence runs before breadth calculation; existing breadth dates are transactional upserts, database failures escape, and seed/update jobs share the advisory lock. The configured `SP500_NDX` value is a configured stock basket, not a claim of complete index membership.
- **API and monitor context:** Public snapshot/history routes are guest-readable, strict, no-store and return unknown consistently for stale or under-covered persisted rows. The monitor keeps ranking, market-state and sector-summary dates distinct. Sector leadership and summary metrics continue to come from the sector scope; the market-state reader remains a separate configured-basket observation. Numeric nullable fields remain explicit and guidance is derived from the canonical state.
- **UI:** `/tools/market-rotation` keeps the Astra brief's existing hierarchy and tokens. It localizes state, breadth condition/confirmation, policy posture and row signals across all three locales, preserves unknown and unavailable values, and renders the source deterministic summary in English with equivalent structured translations elsewhere. The typed `betaAllocation` payload carries the reader's existing policy result without parsing prose. It shows snapshot coverage with its own as-of date, uses the history reader's own latest date, and separates snapshot errors from history loading, empty and error states. History scope changes do not refetch the monitor payload.
- **Browser evidence:** `PLAYWRIGHT_CHANNEL=chrome npx playwright test tests/e2e/market-rotation.spec.ts -g "public monitor handles"` passed 1/1 against controlled fixtures. It covers no-snapshot retry, three locales, localized state/signal labels, keyboard scope changes, history control focus, date provenance, unknown values, source breadth condition/confirmation and the complete typed seven-part summary. The incremental Current read captures are [`current-read-desktop.png`](evidence/market-rotation/current-read-desktop.png) and [`current-read-mobile.png`](evidence/market-rotation/current-read-mobile.png). Earlier controlled evidence covers the real admin-batch→guest path and responsive full-page captures in [`desktop.png`](evidence/market-rotation/desktop.png) and [`mobile.png`](evidence/market-rotation/mobile.png).
- **Verification:** Focused market-state, monitor-contract, monitor-summary and monitor-context unit tests pass (41 tests). The repository-wide typecheck is currently blocked by the unrelated WIP `apps/web/app/routes/price-alerts.tsx` error; re-run `npm run typecheck`, lint and contracts checks after that shared change is repaired. Disposable PostgreSQL and final browser claims remain acceptance gates.

Portfolio20 market context integration remains separate pending work. This document records implementation evidence and does not mark ticket44 complete before independent root/Astra acceptance.

## Astra acceptance — 2026-09-06

Both new Current read captures were independently inspected and accepted, retaining the prior full-page acceptance. Latest disposable PostgreSQL rerun passes all7 state HTTP/batch/context/rotation cases; 41 focused unit tests, owned lint, contracts check and the focused Chrome flow pass. Ticket44 is complete. Portfolio20 was also accepted; earlier pending notes above are historical. Global typecheck awaits the separate ticket34 form fix.

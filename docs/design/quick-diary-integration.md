# Quick Diary integration record

09 remains in progress until the following frozen capabilities have real integrations. No unavailable provider values or empty trade lists are represented as successful data.

Implemented foundation: four structured template kinds and every generator field, 15 market-condition values, five observation types, six original local snippets and account-scoped custom CRUD; civil YMD input; touched-writing preservation; account-scoped 24-hour draft; guarded Ctrl/Cmd J; shared create/append API. The global dialog owns closing and local draft safety and does not register a second React Router blocker alongside a full Diary editor.

## Integration status

- **16 sell ledger → reflection recent trades**: frozen `components/quicknote/QuickNoteTemplateAssistant.vue` calls `GET /api/stats/recent-trades` only for reflection. Response `{ trades: [{ id, symbol, sellDate, sellQuantity, realizedPnL, realizedPnLPct }] }`; selected records populate `QuickNoteTemplateData.relatedTrades`. The shared generator already retains these fields and the exact trade-review Markdown. The selector must use actual owned trade summaries after ledger delivery. [Ticket 16](../../.scratch/diary-v3-rebuild/issues/16-sell-ledger.md) and [ticket 21](../../.scratch/diary-v3-rebuild/issues/21-strategy-performance.md).
- **18 provider → SPX session — implemented**: same assistant invokes `GET /api/market/spx-session` on explicit action, receiving `{ condition, changePercent }`, then patches `marketCondition`. The explicit action in `quick-fields.tsx` now shows pending/error/retry, reports the returned percentage, and preserves manual condition and writing on failure. [Ticket 18](../../.scratch/diary-v3-rebuild/issues/18-market-provider.md).
- **Local capture reminder — implemented in 09**: frozen `composables/useQuickNoteComposer.ts`, `components/QuickReminder.vue`, and `lib/quicknote/quick-reminders.ts` support one local reminder (`reminder1: string | null`), custom time and tomorrow/next-week/next-month presets. Composer checks at initialization and every 30 seconds, reports due reminders and clears them; successful capture clears reminder state. Implemented locally in `quick-capture-tools.tsx`, with account-scoped storage and clear-on-save/logout. This reminder is distinct from the later persisted Diary Alert; it does not depend on ticket 31. [Ticket 31](../../.scratch/diary-v3-rebuild/issues/31-diary-alerts.md).
- **Voice capture — implemented in 09**: frozen `components/VoiceInput.vue` emits a transcript consumed by `appendVoiceTranscript`; it appends with a space and preserves typed text. Web support detection, start/stop, finalized-utterance append, denied/error recovery and typing fallback are implemented. Tests use a controlled browser recognition fixture, not a real microphone. React Native microphone UI remains native-stage work.
- **Stock associations**: frozen composer accepts separate `stockSymbols`, uppercases/deduplicates and caps at ten. These are actual Diary associations, distinct from a trading template's comma-separated symbols that become Markdown. Do not silently accept and discard associations while the relation slice is pending.

Source references above point inside the recorded sanitized archive `docs/parity/source-snapshot.tar.gz`; no live or personal diary data was used.

Final integration status: recent-trade selection and explicit Company context are implemented and verified. The pending-language above records the original dependency contract; final evidence is in `quick-finish-review.md`.

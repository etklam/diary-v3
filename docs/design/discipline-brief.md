# Trading principles — Astra direction

2026-09-06. Tickets35–36. Mode: Operate for the private collection/transfer; Read for the public share. Astra inspected the incumbent desktop collection and mobile public-share evidence before defining this direction. Preserve this existing flat text-first composition and DESIGN.md tokens; no redesign or extra decorative components.

Private page sequence stays heading/context, labelled principle editor, explicit random draw with custom/default provenance, then the ruled collection. Principle content is primary, creation date secondary, followed by native Move up/down, Edit and Delete controls. Keep the established20px editor/action gap,44px targets and wrapped mobile actions. Keyboard reorder returns focus to the moved principle, editing focuses its textarea and failed writes retain text. Large lists must remain fully reachable; no arbitrary new source cap. The random result must never falsely present a fallback quote as the user's saved content.

Transfer belongs after collection controls as an explicit import/export area. Keep native file selection and editable/selectable JSON, a readable preview identifying accepted/skipped/invalid records, the exact source merge/dedup behavior and clear final Import. A preview must not itself write. Export settings distinguish optional author naming from the principle content; the author checkbox remains a20px checkbox in a44px label target. Clipboard failure exposes a selectable URL/text and an accurate recovery message. Failed or uncertain writes retain the proposed data and cannot silently duplicate content; use the source idempotence/dedup or an explicitly documented recovery correction.

Public share stays a narrow text reading surface: title, optional permitted author/Anonymous, rule, principle paragraphs, and Preview import as the final action. Preserve the URL payload and public/private boundaries from the frozen source, validate limits and render text safely. No account email, private identifier or unselected principle is introduced. The OG image uses the same restrained colors, readable text and attribution; escape content, wrap CJK/long text, and inspect the actual generated image rather than only HTTP headers. Public text must remain useful before hydration where the source supports it.

The existing visual composition is approved for continued implementation. Final acceptance needs the pending long-list/ownership/reorder-delete concurrency/uncertain-write checks and actual import→export→public-share→preview/import roundtrip from the tickets. Reuse already passing failure/keyboard evidence; do not rerun unrelated suites or initiate repeated polishing. Astra will inspect the final desktop/mobile transfer pair and generated OG once, with one correction batch if actual defects appear.

# Relative Value and Seasonality — Astra direction

2026-09-06. Tickets 49 and 50 remain unimplemented. Astra defines composition and acceptance; Luna implements. Extend the flat investment research desk in DESIGN.md, using the accepted FIRE input/results rhythm and Company quote provenance. No new visual identity or decorative assets.

## Relative Value (49)

Mode: Operate. The reader compares two instruments, explores constant-ratio price scenarios and records an observation. Begin with a paired input strip: Primary and Comparison each contain a labelled symbol, editable price, its own fetch action and its own quote timestamp/currency/error. On mobile stack the two groups in that order. A compact presets row preserves the four source pairs (^GSPC/SPY, SPY/SPLG, QQQ/QQQM, GLD/GLDM); explanatory text must describe these as presets rather than recommendations.

Place the ratio and inverse ratio directly after inputs with explicit numerator/denominator labels. Then use two columns: target-price controls and scenario table on the left, a wider historical ratio chart on the right. Below 850px stack controls, table, then chart. Retain manual target entry, generated points (count, step, up/down/both), descending target table and the source row-selection action. Give row selection an explicit button; do not make an unlabelled entire row interactive. Display current calculation inputs separately from fetched quote metadata when prices are manually edited. A constant-ratio scenario is a calculation, not a forecast.

Reuse shared market quote/history routes and the existing provider queue/cache. Retain source history ranges 1mo/3mo/6mo/1y/5y/max and alias suggestions. Source joins identical historical timestamps; define comparable observations explicitly and do not silently pair different dates or use display-localized date labels as join keys. Keep unmatched/missing/zero-denominator observations out of numeric ratios, and break chart segments across unavailable observations rather than fabricate continuity. Include a readable date/ratio data table or disclosure for nonvisual access. Show a separate history loading/error/retry region while retaining valid quote/scenario work. Request-generation guards must prevent delayed responses for old symbols from replacing a newer comparison.

Use existing body/label typography, tabular numerals and fine rules; one semantic action color for the chart, labelled axes, no smoothed line that implies extra observed values. At 390px, wide results scroll only inside a named focusable region. Error text sits with the affected side. A failed quote must not attach the previous symbol's quote to the current input. Manual prices remain available and clearly labelled; non-finite, nonpositive and malformed numeric input must not yield Infinity/NaN or accept a numeric prefix of garbage. Preserve valid source formula results: ratio = primary/comparison; inverse = comparison/primary; corresponding target = target/ratio. Bound generated point counts to a reasonable explicit UI limit to avoid unbounded loops, documenting the validation correction.

Copy localized Markdown from the exact displayed inputs/result, including which values are manual versus fetched and their provenance; clipboard failure exposes selectable text. Research capture uses the existing dialog and RELATIVE_VALUE metadata, with primary/comparison symbols, ratio and history range. Preserve its actual Diary/Evidence destination choices, guest-to-login boundary, safe return and failed-save retention. Demonstrate a real persisted capture rather than only dialog opening. Test one-sided quote failure, rapid symbol changes, manual quote override in copied output, zero/missing dates, and desktop/mobile captures in one bounded pair.

## Seasonality (50)

Mode: Read with capture actions. This is the frozen source's fixed 12-month US-market reference dataset, not a symbol-specific live model. Display that scope prominently beside the title and retain the source's translated explanation of the dataset's claimed historical window as source context; do not present it as automatically updated through today's date or independently verified market research.

Lead with current month and next month, each with signed average return, strength and volatility words, plus the selected account timezone. Follow with a compact full-year horizontal bar chart and an accessible 12-row table of return, characteristics and volatility. Use a clear zero reference, signed labels and restrained semantic color; color never substitutes for the values. At desktop width the chart/table can sit beside a narrow interpretation column; on mobile use current/next, full-year data, then interpretation. Do not turn each month into a large card.

The interpretation column preserves strongest three and weakest three months, strong November–April and weak May–October periods, and their translated characteristics/reasons/strategy material. Period figures are arithmetic monthly means, not compounded or cumulative six-month return; label them accordingly. Preserve fixed source values and stable ordering of the February/August tie. Current/next month must be derived from the same injected instant and user timezone, update after a timezone preference changes and handle December→January. No new database/API/provider is needed for this fixed analysis.

Reuse source translations for all descriptive material and localized Markdown; missing translation/content must not expose raw keys or invent analysis. Copy includes all 12 rows and original context. Clipboard failure provides selectable output. Source research capture explicitly sets allowCompanyEvidence=false: preserve that destination boundary for a broad-market reference, using real Diary capture rather than forcing an arbitrary company. Retain SEASONALITY metadata and the localized current-month/strongest/weakest observation. Ticket50's general Diary/Evidence wording does not override this explicit source boundary; record it in acceptance evidence.

Validation: deterministic fixed values/means, timezone boundary and preference change, December rollover, stable strongest/weakest ordering, all three localized copy bodies, guest/login and actual Diary create/append through the existing capture flow. Astra inspects desktop/light and 390px/dark together, with at most one correction pass for demonstrated defects.

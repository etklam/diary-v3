# Public content finish review

Ticket 54–56 implementation is in progress. The bounded vertical slice now has a PostgreSQL `posts` table and migration, shared Zod contracts, admin/public HTTP routes, safe Markdown rendering, SSR article routes, `/blog` compatibility redirects, and a public sitemap response. Admin responses include the author email only inside the role-protected surface; public list/detail responses contain only the public author identity.

The API keeps the source boundary measured in `docs/parity/post-fulltext-probe.json` and `docs/parity/post-fulltext-boolean-probe.json`: public search reads title and excerpt through a combined vector, excludes content, preserves Boolean required/optional/excluded terms, keeps quoted phrases inside one field, and applies the observed short-word, prefix, stop-word and accent behavior. Admin title/author filters remain substring filters.

## Current evidence

- `npx vitest run tests/integration/posts.test.ts --reporter dot`: **7/7 passed** against disposable PostgreSQL. Coverage includes long draft persistence/reopen, generated excerpt, public projection privacy, cross-admin authorization, lifecycle timestamps, source full-text baseline, every frozen Boolean probe case, permissions and bulk operations.
- `npm run contracts:check`: **passed** after registering the Post request/response schemas and routes.
- `npx tsc --noEmit --pretty false`: **passed** with the public SSR and admin route modules present.

Browser evidence for the editor, publish-to-public path, archive hiding, no-JavaScript article reading, three locales and desktop/mobile captures remains pending root acceptance. No production publishing or deployment is claimed here.

## Focused browser evidence — 2026-09-06

- `PLAYWRIGHT_CHANNEL=chrome npx playwright test tests/e2e/posts.spec.ts --reporter dot`: **1/1 passed in 9.1s** using the disposable PostgreSQL E2E server. The case covers Admin draft persistence, publish, a JavaScript-disabled public article response with body/title/canonical/OG assertions, archive hiding, republish, 390px dark overflow and ordinary-user denial.
- [`1440.png`](evidence/public-content/1440.png) and [`390.png`](evidence/public-content/390.png) were regenerated by the passing case. The browser runner ports were released after completion.

The final review must inspect that first HTML contains article body, title, canonical and OG metadata, that drafts/archived/unknown slugs disclose no content, and that the admin form retains all values after an unsuccessful write. The current implementation uses the existing safe Markdown renderer and named local table scroll region.

import { Evidence } from '../evidence';
import { companyContextCopy } from '../company-context-input';
import { reviewScheduleCopy } from '../review-scheduling';
import type { LedgerTransactionResponse } from '@diary/contracts/ledger';
import { ledgerCopy,ledgerEmotions } from '../ledger-copy';
import '../ledger.css';
import '../diary-editor.css';
import { apiFailure,FailureNotice,invalidField,type Failure } from '../api-error';
import { useEffect,useRef,useState } from 'react';
import { Link,useLocation,useNavigate,useParams } from 'react-router';
import { api,useUi } from '../ui';
import { signInPath } from '../session';
import { diaryCopy } from '../diary-copy';
import { Markdown } from '../markdown';
import type { DiaryFields } from '../diary-editor';
type Diary=Omit<DiaryFields,'content'> & {id:string;content:string|null;createdVia?:'WEB'|'API_KEY'|'TELEGRAM_BOT';createdByLabel?:string|null;transactions?:LedgerTransactionResponse[];reviewedAt?:string|null;reviewOutcome?:'INTACT'|'PARTIAL'|'INVALIDATED'|'UNCLEAR'|null};
export default function DiaryPage(){const {t,locale}=useUi();const labels=diaryCopy[locale];const {id}=useParams();const location=useLocation();const navigate=useNavigate();const dialog=useRef<HTMLDialogElement>(null);const [diary,setDiary]=useState<Diary|null>(null);const [error,setError]=useState<Failure|null>(null);const [pending,setPending]=useState(true);const [deleting,setDeleting]=useState(false);const [timezone,setTimezone]=useState<string|null>(null);const reviewLabels=reviewScheduleCopy[locale];const reviewDue=diary?.reviewDueAt?new Date(diary.reviewDueAt):null;const reviewOverdue=reviewDue!==null&&reviewDue.getTime()<=Date.now();const reviewDueText=reviewDue?new Intl.DateTimeFormat(locale,{dateStyle:'medium',timeZone:timezone??'UTC'}).format(reviewDue)+(timezone?'':' UTC'):null;
 async function load(){setPending(true);setError(null);setDiary(null);api.GET('/api/auth/me').then(me=>{if(me.response.ok&&me.data)setTimezone(me.data.data.timezone);}).catch(()=>{});try{const result=await api.GET('/api/diaries/{id}',{params:{path:{id:id??''}}});if(result.response.ok&&result.data)setDiary({...result.data,thesis:result.data.thesis??null,risk:result.data.risk??null,execution:result.data.execution??null});else setError(apiFailure(result.error,t(result.response.status===404||result.response.status===403?'notFound':'failed')));}catch{setError(apiFailure(null,t('connection')));}finally{setPending(false);}}
 async function remove(){setDeleting(true);try{const result=await api.DELETE('/api/diaries/{id}',{params:{path:{id:id??''}}});if(result.response.ok){dialog.current?.close();setDiary(null);navigate('/diaries/new',{replace:true,state:{deleted:true}});}else{dialog.current?.close();setError(apiFailure(result.error,t('failed')));}}catch{dialog.current?.close();setError(apiFailure(null,t('connection')));}finally{setDeleting(false);}}
 useEffect(()=>{void load();},[id]);
 return <article className="diary-reading">{pending?<p role="status">{t('loading')}</p>:error?<><h1>{t('readTitle')}</h1><FailureNotice failure={error}/>{invalidField(error,'transactions')&&<p className="error">{ledgerCopy[locale].oversell}</p>}<button onClick={()=>void load()}>{t('retry')}</button><Link className="inline-link" to={signInPath(`/diaries/${id}`)}>{t('login')}</Link></>:diary&&diary.id===id&&<>{location.state?.saved&&<p className="success" role="status">{t('saved')}</p>}<header><time dateTime={diary.date}>{diary.date}</time><h1>{diary.title}</h1>{diary.createdVia&&diary.createdVia!=='WEB'&&<p data-testid="diary-source">{diary.createdVia==='API_KEY'?(locale==='en'?'Created via API key':locale==='zh-CN'?'通过 API 密钥创建':'透過 API 金鑰建立'):'Telegram'}{diary.createdByLabel?` · ${diary.createdByLabel}`:''}</p>}<ul className="diary-tags" aria-label={labels.tags}>{diary.tags.map(tag=><li key={tag}>{tag}</li>)}</ul>{Boolean(diary.stockSymbols?.length)&&<div className="diary-companies"><h2>{companyContextCopy[locale].label}</h2><ul>{diary.stockSymbols?.map(symbol=><li key={symbol}><Link to={`/stocks/${encodeURIComponent(symbol)}`}>{symbol}</Link></li>)}</ul></div>}</header><Markdown>{diary.content??''}</Markdown><section className="original-reasoning"><h2>{labels.original}</h2><dl>{(['thesis','risk','execution'] as const).map(field=><div key={field}><dt>{labels[field]}</dt><dd>{diary[field]||labels.empty}</dd></div>)}</dl></section>{Boolean(diary.transactions?.length)&&<section className="ledger-reading"><h2>{ledgerCopy[locale].title}</h2>{diary.transactions?.map(transaction=><section key={transaction.id}><h3>{transaction.symbol} · {transaction.type==='BUY'?ledgerCopy[locale].buy:ledgerCopy[locale].sell}</h3><dl>{(['quantity','price','tradeDate','strategy','emotion','notes'] as const).map(field=><div key={field}><dt>{ledgerCopy[locale][field]}</dt><dd>{field==='tradeDate'?new Intl.DateTimeFormat(locale,{dateStyle:'medium',timeStyle:'short',timeZoneName:undefined}).format(new Date(transaction.tradeDate))+' · '+Intl.DateTimeFormat().resolvedOptions().timeZone:field==='emotion'&&transaction.emotion&&Object.hasOwn(ledgerEmotions[locale],transaction.emotion)?ledgerEmotions[locale][transaction.emotion as keyof typeof ledgerEmotions.en]:transaction[field]||'—'}</dd></div>)}</dl></section>)}<Link className="inline-link" to="/stocks">{ledgerCopy[locale].view}</Link></section>}<section className="original-reasoning"><h2>{reviewLabels.review}</h2>{diary.reviewStatus==='reviewed'?<><p>{reviewLabels.reviewed}{diary.reviewOutcome?` · ${reviewLabels[diary.reviewOutcome]}`:''}</p><Link className="button secondary" to={`/diaries/${id}/review`}>{reviewLabels.viewReview}</Link></>:reviewDue?<><p>{reviewOverdue?<strong>{reviewLabels.dueNow}</strong>:`${reviewLabels.duePrefix} `}<time dateTime={reviewDue.toISOString()}>{reviewDueText}</time></p><Link className={reviewOverdue?'button':'button secondary'} to={`/diaries/${id}/review`}>{reviewOverdue?reviewLabels.reviewNow:reviewLabels.review}</Link></>:<><p>{reviewLabels.noneState}</p><Link className="button secondary" to={`/diaries/${id}/edit`}>{reviewLabels.schedule}</Link></>}</section><Evidence key={diary.id} source={{title:diary.title,path:`/diaries/${diary.id}`}}/><footer><div className="diary-actions"><Link className="button secondary" to={`/diaries/${id}/edit`}>{labels.edit}</Link><button type="button" className="danger-button" onClick={()=>dialog.current?.showModal()}>{labels.remove}</button><Link className="button secondary" to="/diaries/new">{t('write')}</Link></div></footer><dialog ref={dialog} className="delete-dialog" aria-labelledby="delete-title"><h2 id="delete-title">{labels.confirm}</h2><p>{diary.title}</p><div className="actions"><button className="secondary" autoFocus onClick={()=>dialog.current?.close()} disabled={deleting}>{labels.cancel}</button><button className="danger-button" disabled={deleting} onClick={()=>void remove()}>{deleting?t('pending'):labels.remove}</button></div></dialog></>}</article>;}

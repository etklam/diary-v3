import { localTradeChoices } from './trade-time';
import { useUi } from './ui';
export const reviewScheduleCopy={en:{due:'Review due at',hint:'Optional. Uses your device timezone:',review:'Review diary',none:'Not scheduled',pending:'Review pending',reviewed:'Reviewed',invalid:'Choose a valid review time. If the clock repeats, select the UTC instant.'},'zh-TW':{due:'複盤時間',hint:'選填。使用你的裝置時區：',review:'複盤日記',none:'未安排複盤',pending:'待複盤',reviewed:'已複盤',invalid:'請選擇有效的複盤時間。時間重複時請選擇 UTC 時刻。'},'zh-CN':{due:'复盘时间',hint:'选填。使用你的设备时区：',review:'复盘日记',none:'未安排复盘',pending:'待复盘',reviewed:'已复盘',invalid:'请选择有效的复盘时间。时间重复时请选择 UTC 时刻。'}};
export function ReviewScheduling({value,instant,onChange}:{value:string;instant:string;onChange:(value:string,instant:string)=>void}){const {locale}=useUi();const l=reviewScheduleCopy[locale];return <fieldset className="original-fields"><legend>{l.review}</legend><p className="muted">{l.hint} {Intl.DateTimeFormat().resolvedOptions().timeZone}</p><label>{l.due}<input type="datetime-local" value={value} onChange={event=>onChange(event.target.value,'')}/></label>{localTradeChoices(value,instant).length>1&&<label>UTC<select required value={instant} onChange={event=>onChange(value,event.target.value)}><option value="">—</option>{localTradeChoices(value,instant).map(item=><option key={item} value={item}>{item}</option>)}</select></label>}</fieldset>;}
